export enum AppRoute {
  Root = "/",
  NotFound = "/*",
  Login = "/login",
  Register = "/register",
  Profile = "/profile/:id",
  Requests = "/requests",
  Catalog = "/catalog",
  Book = "/book/:id",
}
